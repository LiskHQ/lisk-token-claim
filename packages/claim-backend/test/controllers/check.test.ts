import { expect } from 'chai';

describe('check', () => {
	it('should return 400 when address is not valid', () => {
		expect(true).to.be.true;
	});
	it('should return 400 when address is not in leafMap, ie. not eligible', () => {
		expect(true).to.be.true;
	});
	it('should return 200 for address with eligible claim', () => {
		expect(true).to.be.true;
	});
});
